package org.example.models;

public class Duck extends Animal {
    public Duck(int lifespan, int chanceOfGettingSick, int foodConsumption, int foodProduction) {
        super(lifespan, chanceOfGettingSick, foodConsumption, foodProduction);
    }
}
